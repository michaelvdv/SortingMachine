
public class main_thread {
	
	public enum State { 
		INITIAL, START, PUSH, DETECT, // complete list of states the software goes through
		SORT, RESET, PAUSE, ABORT 
	}
	
	State state = State.INITIAL;
	
	boolean startPause;
	boolean abort;
	boolean 
	
	public void main(){
		while (true) {
			switch(state) {
			case INITIAL: 
				
			case START:
				
			case PUSH:
				
			case DETECT:
				
			case SORT:
			
			case RESET:
				
			case PAUSE:
				
			case ABORT:

			}
		}	 
	}
}
